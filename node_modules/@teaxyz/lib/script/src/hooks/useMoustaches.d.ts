import SemVer from "../utils/semver.js";
import { Package, Installation } from "../types.js";
declare function tokenizePackage(pkg: Package): {
    from: string;
    to: string;
}[];
declare function tokenizeVersion(version: SemVer, prefix?: string): {
    from: string;
    to: string;
}[];
declare function tokenizeHost(): {
    from: string;
    to: string;
}[];
declare function apply(input: string, map: {
    from: string;
    to: string;
}[]): string;
export default function (): {
    apply: typeof apply;
    tokenize: {
        deps: (deps: Installation[]) => {
            from: string;
            to: string;
        }[];
        tea: () => {
            from: string;
            to: string;
        }[];
        all: (pkg: Package, deps_: Installation[]) => {
            from: string;
            to: string;
        }[];
        version: typeof tokenizeVersion;
        host: typeof tokenizeHost;
        pkg: typeof tokenizePackage;
    };
};
export {};
