import Path from "../utils/Path.js";
interface Logger {
    syncing(path: Path): void;
    syncd(path: Path): void;
}
export default function (logger?: Logger): Promise<void>;
export interface RunOptions {
    cmd: (string | Path)[];
}
export {};
