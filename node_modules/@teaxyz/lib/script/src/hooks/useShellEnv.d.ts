import { Installation } from "../types.js";
export declare const EnvKeys: readonly ["PATH", "MANPATH", "PKG_CONFIG_PATH", "LIBRARY_PATH", "LD_LIBRARY_PATH", "CPATH", "XDG_DATA_DIRS", "CMAKE_PREFIX_PATH", "DYLD_FALLBACK_LIBRARY_PATH", "SSL_CERT_FILE", "LDFLAGS", "TEA_PREFIX", "ACLOCAL_PATH"];
export type EnvKey = typeof EnvKeys[number];
interface Options {
    installations: Installation[];
}
export default function (): {
    map: typeof map;
    expand: typeof expand;
    flatten: typeof flatten;
};
declare function map({ installations }: Options): Promise<Record<string, string[]>>;
export declare function expand(env: Record<string, string[]>): string;
export declare function flatten(env: Record<string, string[]>): Record<string, string>;
export {};
