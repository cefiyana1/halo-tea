import { Package, PackageRequirement } from "../types.js";
import SemVer from "../utils/semver.js";
import "../utils/misc.js";
export interface Inventory {
    [project: string]: {
        [platform: string]: {
            [arch: string]: string[];
        };
    };
}
export default function useInventory(): {
    select: (rq: Package | PackageRequirement) => Promise<SemVer | undefined>;
    get: (rq: Package | PackageRequirement) => Promise<SemVer[]>;
};
export declare const _internals: {
    get: (rq: PackageRequirement | Package) => Promise<SemVer[]>;
};
