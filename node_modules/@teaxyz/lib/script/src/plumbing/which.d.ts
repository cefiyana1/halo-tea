import { PackageRequirement } from "../types.js";
export type WhichResult = PackageRequirement & {
    shebang: string[];
};
export default function (arg0: string, opts?: {
    providers: boolean;
}): Promise<WhichResult | undefined>;
