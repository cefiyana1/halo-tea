/// <reference types="node" />
import { Package, Installation } from "../types.js";
import Path from "../utils/Path.js";
export default function install(pkg: Package, logger?: Logger): Promise<Installation>;
export interface Logger {
    locking?(pkg: Package): void;
    downloading?(info: {
        pkg: Package;
        src?: URL;
        dst?: Path;
        rcvd?: number;
        total?: number;
    }): void;
    installing?(info: {
        pkg: Package;
        progress: number | undefined;
    }): void;
    unlocking?(pkg: Package): void;
    installed?(installation: Installation): void;
}
export declare function ConsoleLogger(prefix?: any): Logger;
