import { Logger as BaseLogger } from "../plumbing/install.js";
import { Installation, PackageSpecification } from "../types.js";
import { Resolution } from "../plumbing/resolve.js";
export interface Logger extends BaseLogger {
    resolved?(resolution: Resolution): void;
    progress?(completion: number): void;
}
export declare function ConsoleLogger(prefix?: any): Logger;
export default function (pkgs: PackageSpecification[] | string[] | string, logger?: Logger): Promise<Installation[]>;
