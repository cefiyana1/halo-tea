import { SupportedPlatform, SupportedArchitecture } from "./utils/host.js";
import SemVer, { Range } from "./utils/semver.js";
import Path from "./utils/Path.js";
export interface Package {
    project: string;
    version: SemVer;
}
export interface PackageRequirement {
    project: string;
    constraint: Range;
}
export type PackageSpecification = Package | PackageRequirement;
export interface Installation {
    path: Path;
    pkg: Package;
}
export type Stowage = {
    type: 'src';
    pkg: Package;
    extname: string;
} | {
    type: 'bottle';
    pkg: Package;
    compression: 'xz' | 'gz';
    host?: {
        platform: SupportedPlatform;
        arch: SupportedArchitecture;
    };
};
export type Stowed = Stowage & {
    path: Path;
};
export declare function StowageNativeBottle(opts: {
    pkg: Package;
    compression: 'xz' | 'gz';
}): Stowage;
