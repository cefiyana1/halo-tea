import { PackageRequirement, Package } from "../types.js";
interface ReturnValue {
    pkgs: PackageRequirement[];
    dry: PackageRequirement[];
    wet: PackageRequirement[];
    bootstrap_required: Set<string>;
}
export default function hydrate(input: (PackageRequirement | Package)[] | (PackageRequirement | Package), get_deps?: (pkg: PackageRequirement, dry: boolean) => Promise<PackageRequirement[]>): Promise<ReturnValue>;
export {};
